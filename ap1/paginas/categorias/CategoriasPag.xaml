<Page x:Class="POS.paginas.categoria.CategoriasPag"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Updated header to match productos style -->
        <Border Grid.Row="0" Background="#8b5cf6" Padding="30,0">
            <Grid>
                <TextBlock Text="Gestión de Categorías" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="White"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Changed layout to have form on left and DataGrid on right -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Added form container on the left -->
            <Border Grid.Column="0" 
                   BorderBrush="Transparent" 
                   BorderThickness="0" 
                   Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Nueva Categoría" 
                                  FontSize="22" 
                                  FontWeight="Bold" 
                                  Foreground="#8b5cf6"
                                  Margin="0,0,0,20"/>

                        <TextBlock Text="Nombre de la Categoría *" 
                                  FontSize="14" 
                                  FontWeight="SemiBold"
                                  Margin="0,0,0,8"/>
                        <TextBox x:Name="NombreCategoriaTextBox"
                                BorderBrush="#e0e0e0" 
                                BorderThickness="1" 
                                Padding="10"
                                FontSize="14"
                                Margin="0,0,0,15"
                                Height="40"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0"
                                   Content="Guardar" 
                                   Background="#8B5CF6" 
                                   Foreground="White"
                                   FontWeight="Bold"
                                   Padding="15,10"
                                   BorderThickness="0"
                                   Cursor="Hand"
                                   Click="GuardarCategoria_Click" Grid.ColumnSpan="3"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Moved DataGrid to the right side -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Border BorderBrush="#e0e0e0" 
                           BorderThickness="1" 
                           Background="White"
                           Padding="15,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" 
                                      Text="🔍" 
                                      FontSize="18" 
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"
                                      Foreground="#999"/>
                            <TextBox Grid.Column="1" 
                                    x:Name="SearchTextBox"
                                    BorderThickness="0" 
                                    FontSize="14"
                                    VerticalAlignment="Center"
                                    Foreground="#666"
                                    Height="40"
                                    Text="Buscar categorías..."/>
                        </Grid>
                    </Border>
                </Grid>

                <Border Grid.Row="1" 
                       BorderBrush="#e0e0e0" 
                       BorderThickness="1"
                       Background="White">
                    <DataGrid x:Name="CategoriasDataGrid"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserResizeRows="False"
                             IsReadOnly="True"
                             HeadersVisibility="Column"
                             GridLinesVisibility="None"
                             BorderThickness="0"
                             Background="White"
                             RowBackground="White"
                             AlternatingRowBackground="White"
                             SelectionMode="Single"
                             SelectionUnit="FullRow"
                             RowHeight="50">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#333"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#e0e0e0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="15,0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#333"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                   BorderThickness="0"
                                                   Padding="15,0">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#f0f0f0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#f9f9f9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Width="80" Binding="{Binding Id}"/>
                            <DataGridTextColumn Header="Nombre" Width="*" Binding="{Binding Nombre}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Acciones" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <ToggleButton x:Name="ActionsToggleButton"
                                                         Content="⋯" 
                                                         FontSize="20"
                                                         FontWeight="Bold"
                                                         Width="30" 
                                                         Height="30" 
                                                         Background="Transparent"
                                                         BorderThickness="0"
                                                         Foreground="#666"
                                                         Cursor="Hand"
                                                         HorizontalAlignment="Center"/>

                                            <Popup IsOpen="{Binding IsChecked, ElementName=ActionsToggleButton}"
                                                   StaysOpen="False"
                                                   PlacementTarget="{Binding ElementName=ActionsToggleButton}"
                                                   Placement="Bottom"
                                                   AllowsTransparency="True">
                                                <Border Background="White"
                                                       BorderBrush="#e0e0e0"
                                                       BorderThickness="1"
                                                       Padding="5">
                                                    <StackPanel>
                                                        <Button Content="✏️ Editar"
                                                               Background="Transparent"
                                                               BorderThickness="0"
                                                               Padding="15,8"
                                                               FontSize="14"
                                                               HorizontalContentAlignment="Left"
                                                               Cursor="Hand"
                                                               Click="EditarCategoria_Click"
                                                               Tag="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#f0f0f0"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        <Button Content="🗑️ Borrar"
                                                               Background="Transparent"
                                                               BorderThickness="0"
                                                               Padding="15,8"
                                                               FontSize="14"
                                                               HorizontalContentAlignment="Left"
                                                               Cursor="Hand"
                                                               Foreground="#ef4444"
                                                               Click="EliminarCategoria_Click"
                                                               Tag="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#fee"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </StackPanel>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <TextBlock x:Name="CategoriaCountText"
                              Text="Mostrando 0 de 0 categorías" 
                              FontSize="14" 
                              Foreground="#666"
                              VerticalAlignment="Center"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
